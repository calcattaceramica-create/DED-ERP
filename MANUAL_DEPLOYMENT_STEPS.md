# 📋 دليل النشر اليدوي - خطوة بخطوة

---

## ⚠️ **ملاحظة مهمة:**

السيرفر يحتاج إلى **كلمة مرور** للاتصال. لا يمكنني إدخال كلمة المرور تلقائياً لأسباب أمنية.

**يجب عليك تنفيذ الخطوات يدوياً.**

---

## 🚀 **الطريقة الأسهل: استخدام WinSCP + PuTTY**

---

### **الخطوة 1: تحميل الأدوات**

#### **أ. WinSCP (لرفع الملفات):**
1. زيارة: https://winscp.net/eng/download.php
2. تحميل: **Installation package**
3. تثبيت البرنامج

#### **ب. PuTTY (للاتصال بالسيرفر):**
1. زيارة: https://www.putty.org/
2. تحميل: **putty.exe** أو **installer**
3. تثبيت البرنامج

---

### **الخطوة 2: رفع الملفات باستخدام WinSCP**

1. **افتح WinSCP**

2. **أدخل معلومات الاتصال:**
   ```
   File protocol: SFTP
   Host name: 147.79.102.91
   Port number: 22
   User name: root
   Password: [كلمة مرور السيرفر]
   ```

3. **اضغط Login**

4. **ارفع هذه الملفات:**
   
   من الجانب الأيسر (جهازك):
   ```
   C:\Users\DELL\DED\erp_deploy.zip
   C:\Users\DELL\DED\deploy_erp_improved.sh
   C:\Users\DELL\DED\.env.production
   ```
   
   إلى الجانب الأيمن (السيرفر):
   ```
   /root/
   ```
   
   **طريقة الرفع:**
   - اسحب الملفات من اليسار إلى اليمين
   - أو انقر بزر الماوس الأيمن → Upload

5. **انتظر حتى يكتمل الرفع** (2-5 دقائق)

---

### **الخطوة 3: تنفيذ النشر باستخدام PuTTY**

1. **افتح PuTTY**

2. **أدخل معلومات الاتصال:**
   ```
   Host Name: 147.79.102.91
   Port: 22
   Connection type: SSH
   ```

3. **اضغط Open**

4. **سجّل الدخول:**
   ```
   login as: root
   password: [كلمة مرور السيرفر]
   ```

5. **نفّذ الأوامر التالية واحداً تلو الآخر:**

   ```bash
   # الانتقال للمجلد الرئيسي
   cd /root
   
   # التحقق من وجود الملفات
   ls -lh erp_deploy.zip deploy_erp_improved.sh .env.production
   
   # فك ضغط المشروع
   unzip -o erp_deploy.zip -d erp
   
   # نسخ ملف الإعدادات
   cp /root/.env.production /root/erp/.env.production
   
   # جعل السكريبت قابل للتنفيذ
   chmod +x deploy_erp_improved.sh
   
   # تشغيل النشر
   ./deploy_erp_improved.sh
   ```

6. **انتظر حتى يكتمل النشر** (10-15 دقيقة)

---

## 🎯 **الطريقة البديلة: استخدام PowerShell**

إذا كنت تفضل استخدام PowerShell:

### **1. افتح PowerShell كمسؤول**
- اضغط `Win + X`
- اختر **Windows PowerShell (Admin)**

### **2. نفّذ الأوامر:**

```powershell
# رفع الملف المضغوط
scp C:\Users\DELL\DED\erp_deploy.zip root@147.79.102.91:/root/
# [ستُطلب كلمة المرور]

# رفع سكريبت النشر
scp C:\Users\DELL\DED\deploy_erp_improved.sh root@147.79.102.91:/root/
# [ستُطلب كلمة المرور]

# رفع ملف الإعدادات
scp C:\Users\DELL\DED\.env.production root@147.79.102.91:/root/
# [ستُطلب كلمة المرور]

# الاتصال بالسيرفر
ssh root@147.79.102.91
# [ستُطلب كلمة المرور]
```

### **3. على السيرفر:**

```bash
cd /root
unzip -o erp_deploy.zip -d erp
cp /root/.env.production /root/erp/.env.production
chmod +x deploy_erp_improved.sh
./deploy_erp_improved.sh
```

---

## 📊 **ما يحدث أثناء النشر:**

عند تشغيل `./deploy_erp_improved.sh`، سيقوم السكريبت بـ:

1. ✅ تحديث النظام
2. ✅ تثبيت Python 3
3. ✅ تثبيت PostgreSQL
4. ✅ تثبيت Nginx
5. ✅ تثبيت Certbot (للـ SSL)
6. ✅ إنشاء قاعدة البيانات
7. ✅ إنشاء البيئة الافتراضية
8. ✅ تثبيت المكتبات
9. ✅ إنشاء خدمة systemd
10. ✅ إعداد Nginx
11. ✅ تفعيل HTTPS
12. ✅ إعداد Health Check
13. ✅ إعداد النسخ الاحتياطي

**المدة المتوقعة:** 10-15 دقيقة

---

## ✅ **التحقق من نجاح النشر:**

بعد اكتمال السكريبت، نفّذ:

```bash
# حالة التطبيق
systemctl status erp

# حالة Nginx
systemctl status nginx

# حالة PostgreSQL
systemctl status postgresql
```

**إذا كانت جميع الخدمات تعمل (active/running)، فالنشر نجح!** ✅

---

## 🌐 **الوصول للتطبيق:**

### **1. افتح المتصفح:**
```
https://srv1392516.hstgr.cloud
```

### **2. سجّل الدخول:**
```
Username: admin
Password: admin123
```

### **3. غيّر كلمة المرور فوراً!** ⚠️

---

## 🆘 **استكشاف الأخطاء:**

### **مشكلة: الموقع لا يفتح**

```bash
# تحقق من السجلات
journalctl -u erp -n 100

# تحقق من Nginx
nginx -t
systemctl status nginx

# تحقق من التطبيق
systemctl status erp
```

### **مشكلة: خطأ في قاعدة البيانات**

```bash
# تحقق من PostgreSQL
systemctl status postgresql

# تحقق من الاتصال
sudo -u postgres psql -c "\l"
```

### **مشكلة: SSL لا يعمل**

```bash
# تحقق من الشهادة
certbot certificates

# تجديد الشهادة
certbot renew --dry-run
```

---

## 📞 **الدعم:**

راجع الملفات:
- `DEPLOYMENT_GUIDE.md` - دليل شامل
- `DEPLOYMENT_CHECKLIST.md` - قائمة تحقق
- `DEPLOYMENT_SUMMARY.md` - ملخص سريع

---

## 🎊 **ملخص الخطوات:**

1. ✅ **حمّل WinSCP و PuTTY**
2. ✅ **ارفع الملفات باستخدام WinSCP:**
   - `erp_deploy.zip`
   - `deploy_erp_improved.sh`
   - `.env.production`
3. ✅ **اتصل بالسيرفر باستخدام PuTTY**
4. ✅ **نفّذ الأوامر:**
   ```bash
   cd /root
   unzip -o erp_deploy.zip -d erp
   cp /root/.env.production /root/erp/.env.production
   chmod +x deploy_erp_improved.sh
   ./deploy_erp_improved.sh
   ```
5. ✅ **انتظر 10-15 دقيقة**
6. ✅ **زيارة:** `https://srv1392516.hstgr.cloud`

---

**🚀 حظاً موفقاً في النشر!**

