# 🔑 إعداد SSH Key للنشر التلقائي

---

## 🎯 **الهدف:**
إنشاء SSH key لتمكين الاتصال بالسيرفر بدون كلمة مرور

---

## 📋 **الخطوات:**

### **الخطوة 1: إنشاء SSH Key على Windows**

افتح **PowerShell** ونفّذ:

```powershell
# إنشاء SSH key
ssh-keygen -t ed25519 -C "erp-deployment-key"
```

**عند السؤال:**
- `Enter file in which to save the key`: اضغط **Enter** (سيحفظ في المسار الافتراضي)
- `Enter passphrase`: اضغط **Enter** (بدون كلمة مرور)
- `Enter same passphrase again`: اضغط **Enter**

---

### **الخطوة 2: عرض المفتاح العام**

```powershell
# عرض المفتاح العام
cat $env:USERPROFILE\.ssh\id_ed25519.pub
```

**انسخ الناتج** (سيبدأ بـ `ssh-ed25519 ...`)

---

### **الخطوة 3: إضافة المفتاح للسيرفر**

#### **أ. باستخدام Hostinger Panel:**

1. اذهب إلى: **VPS → SSH key** (في القائمة الجانبية)
2. اضغط **Add SSH key**
3. الصق المفتاح العام
4. احفظ

#### **ب. أو باستخدام SSH مباشرة:**

```powershell
# اتصل بالسيرفر (ستُطلب كلمة المرور مرة واحدة فقط)
ssh root@147.79.102.91

# على السيرفر، نفّذ:
mkdir -p ~/.ssh
chmod 700 ~/.ssh
nano ~/.ssh/authorized_keys
```

**الصق المفتاح العام** في الملف، ثم:
- اضغط `Ctrl + O` (حفظ)
- اضغط `Enter`
- اضغط `Ctrl + X` (خروج)

```bash
# تعيين الصلاحيات
chmod 600 ~/.ssh/authorized_keys
exit
```

---

### **الخطوة 4: اختبار الاتصال**

```powershell
# يجب أن يتصل بدون طلب كلمة مرور
ssh root@147.79.102.91
```

إذا نجح الاتصال بدون كلمة مرور، فقد تم الإعداد بنجاح! ✅

---

## 🚀 **بعد إعداد SSH Key:**

سأتمكن من تنفيذ النشر تلقائياً بدون الحاجة لكلمة مرور!

---

## 📞 **إذا واجهت مشاكل:**

### **مشكلة: المفتاح غير موجود**
```powershell
# تحقق من وجود المفتاح
ls $env:USERPROFILE\.ssh\
```

### **مشكلة: ما زال يطلب كلمة مرور**
```bash
# على السيرفر، تحقق من الصلاحيات
ls -la ~/.ssh/
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

---

**بعد إعداد SSH Key، أخبرني وسأبدأ النشر فوراً!** 🚀

