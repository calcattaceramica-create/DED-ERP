# 🚀 النشر عبر Hostinger Panel (الطريقة الأسرع!)

---

## ✅ **الخطوات (بدون الحاجة لـ SSH من جهازك):**

---

### **الخطوة 1: رفع الملفات عبر File Manager**

1. **اذهب إلى Hostinger Panel:**
   - https://hpanel.hostinger.com/

2. **افتح File Manager:**
   - من القائمة الجانبية: **VPS → File Manager**
   - أو: **VPS → Files**

3. **انتقل إلى `/root/`:**
   - في شريط المسار، اكتب: `/root/`

4. **ارفع الملفات:**
   - اضغط **Upload**
   - اختر هذه الملفات من جهازك:
     ```
     C:\Users\DELL\DED\erp_deploy.zip
     C:\Users\DELL\DED\deploy_erp_improved.sh
     C:\Users\DELL\DED\.env.production
     ```
   - انتظر حتى يكتمل الرفع

---

### **الخطوة 2: فتح Terminal في Hostinger**

1. **افتح Browser Terminal:**
   - من القائمة الجانبية: **VPS → Browser terminal**
   - أو ابحث عن زر **Terminal** أو **SSH**

2. **ستفتح نافذة Terminal مباشرة في المتصفح**

---

### **الخطوة 3: تنفيذ النشر**

في Terminal، نفّذ هذه الأوامر **واحداً تلو الآخر:**

```bash
# 1. الانتقال للمجلد الرئيسي
cd /root

# 2. التحقق من وجود الملفات
ls -lh erp_deploy.zip deploy_erp_improved.sh .env.production

# 3. فك ضغط المشروع
unzip -o erp_deploy.zip -d erp

# 4. نسخ ملف الإعدادات
cp /root/.env.production /root/erp/.env.production

# 5. جعل السكريبت قابل للتنفيذ
chmod +x deploy_erp_improved.sh

# 6. تشغيل النشر
./deploy_erp_improved.sh
```

---

### **الخطوة 4: مراقبة التقدم**

- السكريبت سيبدأ في تثبيت جميع المتطلبات
- ستظهر رسائل توضح التقدم
- **لا تغلق النافذة** حتى يكتمل النشر
- **المدة المتوقعة:** 10-15 دقيقة

---

## 📊 **ما يحدث أثناء النشر:**

```
✅ تحديث النظام
✅ تثبيت Python 3
✅ تثبيت PostgreSQL
✅ تثبيت Nginx
✅ تثبيت Certbot (SSL)
✅ إنشاء قاعدة البيانات
✅ إنشاء البيئة الافتراضية
✅ تثبيت المكتبات
✅ إنشاء خدمة systemd
✅ إعداد Nginx
✅ تفعيل HTTPS
✅ إعداد Health Check
✅ إعداد النسخ الاحتياطي
```

---

## ✅ **التحقق من نجاح النشر:**

بعد اكتمال السكريبت، نفّذ في Terminal:

```bash
# حالة التطبيق
systemctl status erp

# حالة Nginx
systemctl status nginx

# حالة PostgreSQL
systemctl status postgresql
```

**إذا كانت جميع الخدمات `active (running)`، فالنشر نجح!** ✅

---

## 🌐 **الوصول للتطبيق:**

### **1. افتح المتصفح:**
```
https://srv1392516.hstgr.cloud
```

### **2. سجّل الدخول:**
```
Username: admin
Password: admin123
```

### **3. غيّر كلمة المرور فوراً!** ⚠️

---

## 🆘 **إذا واجهت مشاكل:**

### **مشكلة: الملفات غير موجودة**
```bash
# تحقق من المسار
pwd
ls -la
```

### **مشكلة: Permission denied**
```bash
# تأكد من أنك في /root
cd /root
chmod +x deploy_erp_improved.sh
```

### **مشكلة: unzip command not found**
```bash
# ثبّت unzip
apt update
apt install -y unzip
```

### **مشكلة: السكريبت توقف**
```bash
# عرض السجلات
tail -f /root/erp/logs/erp.log
journalctl -u erp -n 100
```

---

## 📝 **ملاحظات مهمة:**

1. **لا تغلق Terminal** أثناء تشغيل السكريبت
2. **إذا انقطع الاتصال:**
   - أعد فتح Terminal
   - تحقق من حالة الخدمات
   - أكمل من حيث توقفت

3. **بعد النشر:**
   - غيّر كلمة مرور admin
   - غيّر كلمة مرور PostgreSQL
   - فعّل جدار الحماية (UFW)

---

## 🎯 **الخطوات السريعة (ملخص):**

1. ✅ افتح **Hostinger Panel**
2. ✅ اذهب إلى **File Manager**
3. ✅ ارفع الملفات الثلاثة إلى `/root/`
4. ✅ افتح **Browser Terminal**
5. ✅ نفّذ الأوامر الستة
6. ✅ انتظر 10-15 دقيقة
7. ✅ زيارة `https://srv1392516.hstgr.cloud`

---

**🚀 هذه الطريقة الأسهل والأسرع!**

**لا تحتاج إلى:**
- ❌ تثبيت WinSCP
- ❌ تثبيت PuTTY
- ❌ إعداد SSH keys
- ❌ كلمات مرور

**كل شيء من داخل Hostinger Panel!** 🎉

